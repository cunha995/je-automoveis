# Example env vars for backend
PORT=3000
# SMTP settings (use your provider credentials)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password

# Email to receive contact messages
TO_EMAIL=contato@jeautomoveis.com
# Optional FROM email
FROM_EMAIL=no-reply@jeautomoveis.com

# SendGrid (preferred):
# Generate an API key in SendGrid and paste it here
SENDGRID_API_KEY=

# Admin panel credentials (change in production)
ADMIN_USERNAME=je2026
ADMIN_PASSWORD=Je2026

# Cloudinary (persistent image storage)
# Option A: full URL
# CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME
# Option B: separate vars
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=je-automoveis
