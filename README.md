# JE Automóveis — Site Estático

Protótipo inicial do site para JE Automóveis (HTML/CSS/JS).

Como executar localmente:

1. Abra `index.html` no navegador (duplo clique) ou use um servidor local:

```bash
# usando Python 3
python -m http.server 8000
# então abra http://localhost:8000
```

Próximos passos:
- Substituir textos e adicionar fotos dos veículos
- Implementar envio de formulário por backend (ex: Netlify Functions, Formspree)
- Configurar domínio e deploy (GitHub Pages / Netlify / Vercel)
